{% extends '@BackOffice/base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/errors.css') }}" />
{% endblock %}

{% block body %}
    <div>
        <h1>{{ title }}</h1>
        <p>{{ exception.message }}</p>
    </div>
{% endblock %}