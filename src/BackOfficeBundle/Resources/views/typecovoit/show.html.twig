{% extends '@BackOffice/base.html.twig' %}

{% block body %}
    <h1>Typecovoit</h1>

    <table>
        <tbody>
            <tr>
                <th>Type</th>
                <td>{{ typeCovoit.type }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeCovoit.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typecovoit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typecovoit_edit', { 'id': typeCovoit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
